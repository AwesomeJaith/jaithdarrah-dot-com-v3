services:
  sticker-server:
    build: .
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - PORT=3001
      - NODE_ENV=production
      - TRANSFORMERS_CACHE=/app/.model-cache
    deploy:
      resources:
        limits:
          memory: 4G
